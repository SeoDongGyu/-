def solution(arr):
    a = []
    for i in range(len(arr)):
        if i == 0:
            a.append(arr[i])
        if i >= 1:
            if arr[i-1] != arr[i]:
                a.append(arr[i])
    return a
